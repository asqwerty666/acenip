usage: sfmriprep.pl [-cut <text_file>] [-st] [-fs] <project>

Run fmriprep image over a set of fMRI images

Options:
-cut <text_file>: run only over those subjects
-st: do slicetiming
-fs: reuse FS directory (ignored, this is the default behavior)
<project>: name of the project (mandatory)
-h: print this help

